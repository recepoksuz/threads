/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i.guim.co.uk', 'data:image', 'static.cdninstagram.com', 'i.hizliresim.com'],
      },
}

module.exports = nextConfig
